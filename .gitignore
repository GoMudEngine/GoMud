.vscode
private-notes.txt
_datafiles/**/users/*
_datafiles/**/plugin-data/*
**/config-overrides.yaml
**/.roundcount
**/rooms.instances
**/mobs.instances
_datafiles/**/shops
_datafiles/**/warehouses
# Runtime living-economy state — server-maintained delivery/stock/bounty ledgers
_datafiles/**/caravans/
_datafiles/**/foragers/
_datafiles/**/crates/
_datafiles/**/bounties.yaml
# Runtime faction rep state — not committed
_datafiles/**/factions.rep
# Runtime crime log state — not committed
_datafiles/**/factions.crimes
# Runtime NPC knowledge state — not committed
_datafiles/**/knowledge/
# Runtime NPC opinion state — not committed
_datafiles/**/opinions/
# Runtime NPC fact awareness state — not committed
_datafiles/**/facts.awareness/
# Runtime NPC goal state — not committed
_datafiles/**/goals/
.worktrees/
tools/testing/reports/*.md
vendor/
server.crt
server.key
tmp-gomud.exe
go-mud-server.exe
go-mud-server
GoMud.exe
/dogmud
/dogmud.exe
/dogmud-t3-review.exe
__debug*
.aider*
.claude/settings.local.json
.claude/worktrees/
startup.log
server_boot.log
boot_err.txt
boot_out.txt
smoke_out.txt
GoMud.exe~
_datafiles/logs/
# Player feedback sinks — runtime-appended by the bug/suggest commands
_datafiles/feedback/
internal/usercommands/_datafiles/feedback/
# Test-run runtime residue written under internal package-local datafiles
internal/**/_datafiles/**/knowledge/
internal/**/_datafiles/**/opinions/
bug_log.txt
moons_feedback.md
post_stage18_thoughts.txt
wtmk_cover_concept.png
tools/mud_cmd.txt
tools/mud_log.txt
tools/mud_output.txt
# Bridge runtime artifacts (AI tester scratch + helper script)
tools/bridge_stderr.txt
tools/bridge_stderr2.txt
tools/bridge_stdout.txt
tools/run_bridge.bat
# Coverage output (generated by go test -coverprofile)
cov_*.out
coverage.out
coverage_check.out
# Economy health snapshots — runtime state, regenerated by the server.
_datafiles/**/economy/snapshots/
# CodeGraph index (per-repo MCP server cache)
.codegraph/
# Novel ebook/audiobook build artifacts. The .epub is small enough to track,
# but the audiobook M4B (~340 MB) exceeds GitHub's 100 MB/file limit.
book_build/
*.m4b

# Superpowers brainstorming artifacts
.superpowers/

.icongen/

# Local MCP config — contains a personal API key, never commit
.mcp.json

# Archived loose screenshots / stray images out of repo root (root only —
# tools/_archive/ holds retired source we DO track)
/_archive/

# Playtest harness overlay — transient run state + generated reports
tools/playtest/.run*/
tools/playtest/.d2.log
tools/playtest/reports/*
!tools/playtest/reports/.gitkeep

# Local build/test artifacts + tool logs
/*.exe
__pycache__/
tools/bridge_run.log
tools/mud_bridge_log.txt

# Claude Code harness local state
.claude/projects/
.claude/scheduled_tasks.lock

# Prod player saves pulled for migration analysis — never commit
_archive/prod-users/
_archive/prod-users.tgz

# Stray screenshots dropped in the repo root (see memory)
Screenshot *.png

# Moderation living state (petition queue + ban lists) — persist on prod, never commit
_datafiles/world/dogmud/moderation/
