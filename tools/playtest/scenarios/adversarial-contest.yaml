# ============================================================
# DEFERRED (0.3d) — PvP / adversarial-contest
# ============================================================
# This scenario is intentionally NOT migrated to the 0.3d ephemeral
# roster schema. `playtestrun scenario` / ParseScenario refuse any
# scenario with requires.pvp until allow-listed ephemeral PvP config
# overrides exist. Do not invoke this file with the 0.3d driver.
#
# Historical notes (pre-0.3d): DOGMud PvP is OFF by default and required
# a manual production-style config edit + restart. That path is out of
# scope for shared ephemeral scenarios until overrides land.
# ============================================================

name: adversarial-contest
mode: adversarial
summary: >-
  DEFERRED: two testers meet and engage in PvP combat. Not runnable under
  playtestrun scenario until ephemeral PvP overrides exist.
requires:
  pvp: true
  max_connections: 20

# Legacy roster left for reference only — ParseScenario fails closed on
# requires.pvp before roster validation would matter.
roster:
  - id: red
    personality: bug-finder
    goals: goals/scenarios/adversarial-contest/red.yaml
  - id: blue
    personality: bug-finder
    goals: goals/scenarios/adversarial-contest/blue.yaml

group_goals:
  - id: deferred
    do: Do not run.
    verify: Scenario refused at parse time.
